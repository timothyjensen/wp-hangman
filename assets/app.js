(function(w) {

	// First off, come up with a list of variables that you think we might need to hold data and state. First one is free, and will be used to hold available guesses.

	var guesses,
		characterOptions,
		answer;

})(window);
